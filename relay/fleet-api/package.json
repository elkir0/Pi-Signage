{
  "name": "zaforge-fleet-api",
  "version": "1.0.0",
  "description": "ZAFORGE relay control plane: enrollment, WireGuard peer + mosquitto dynamic-security provisioning, multi-tenant device fleet management (SQLite source of truth).",
  "license": "UNLICENSED",
  "private": true,
  "type": "commonjs",
  "main": "server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "reconcile": "node -e \"require('./reconcile').run()\""
  },
  "dependencies": {
    "better-sqlite3": "^11.8.1",
    "mqtt": "^5.10.1"
  }
}
