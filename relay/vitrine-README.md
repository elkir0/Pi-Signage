# Site vitrine Zaforge (déplacé)

Le site marketing `zaforge.com` a été **extrait dans son propre dépôt** (isolation) :
- **Repo** : https://github.com/elkir0/zaforge-vitrine (local : `~/projects/zaforge-vitrine`)
- **Déploiement** : conteneur LXC **104 `zaforge-vitrine`** sur le bare-metal OVH (`chirosteo`, `10.10.10.104`),
  reverse-proxifié par le conteneur proxy 101 pour `zaforge.com` (le `/impro/` reste sur le proxy).

Ne plus éditer la vitrine ici.
