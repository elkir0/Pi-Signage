#!/usr/bin/expect -f
set timeout 60
spawn ssh -o StrictHostKeyChecking=no pi@192.168.1.106
expect "password:"
send "palmer00\r"
expect "$ "
send "sudo apt update && sudo apt install -y git wget curl vlc chromium-browser\r"
expect "$ "
send "exit\r"
expect eof