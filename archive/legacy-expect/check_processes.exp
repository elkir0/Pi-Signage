#!/usr/bin/expect -f

set timeout 10
spawn ssh -o StrictHostKeyChecking=no pi@192.168.1.103 "ps aux | grep -E 'chromium|vlc|mpv|ffplay' | grep -v grep"

expect {
    "password:" {
        send "raspberry\r"
        exp_continue
    }
    eof
}