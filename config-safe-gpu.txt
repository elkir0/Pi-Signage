# PiSignage GPU Config - SAFE MODE
# Configuration conservatrice pour éviter les crashes

# Mémoire GPU raisonnable
gpu_mem=256

# Driver moderne mais stable
dtoverlay=vc4-kms-v3d

# Fréquences modérées (pas d'overclocking)
# arm_freq=1800  # Commenté pour utiliser défaut
# gpu_freq=600   # Commenté pour utiliser défaut

# HDMI standard
hdmi_group=2
hdmi_mode=82  # 1920x1080 @ 60Hz
hdmi_drive=2
disable_overscan=1

# Performance modérée
max_framebuffers=2

# Note: Si le Pi ne démarre pas avec cette config:
# 1. Retirer la carte SD
# 2. Sur un autre PC, éditer /boot/config.txt
# 3. Commenter ou retirer les lignes problématiques
# 4. Remettre la carte SD et redémarrer